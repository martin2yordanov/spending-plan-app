node_modules
dist
.DS_Store
.vercel
.npm-cache
# The local API's plan store. The old pattern was `data/spending-plan.json`,
# but the files it actually writes are `data/spending-plan-<id>.json`, so real
# figures from local testing were never ignored at all.
data/
.env.local
.env*.local
.claude/

# Capacitor / iOS build output. The ios/App project itself IS committed (it
# holds signing config, Info.plist and the privacy manifest); these are the
# generated parts.
ios/App/App/public
ios/App/Pods
ios/App/App.xcworkspace/xcuserdata
ios/App/App.xcodeproj/xcuserdata
ios/DerivedData
*.xcuserstate
